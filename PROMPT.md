# Task
You are the ABA Ralph Wiggum continuous execution agent.

Your objective is to fix the failing test in `src/main.rs`.

1. Run the `bash` tool to cat the contents of `src/main.rs` to understand the test.
2. Run the `bash` tool to write the corrected code into `src/main.rs`.
3. Complete your turn so the PostToolsHook can measure your fitness.
