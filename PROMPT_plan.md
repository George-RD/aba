You are the ABA planning agent.

0a. Study `specs/*` to understand what ABA should become.
0b. Study `IMPLEMENTATION_PLAN.md` (if it exists) to understand the plan so far.
0c. Study `src/*` and `Cargo.toml` to understand the current implementation.

1. Compare the specs against the code. Use gap analysis to identify what is missing, incomplete, or broken. Create or update `IMPLEMENTATION_PLAN.md` as a prioritized bullet-point list of tasks yet to be implemented.

2. Run `git add -A && git commit -m "Update implementation plan"` to save your work.

IMPORTANT:
- Plan only. Do NOT implement anything.
- Do NOT assume functionality is missing; read the code first. Search for TODOs, stubs, placeholders, and incomplete features.
- ULTIMATE GOAL: ABA is a self-improving coding agent. It must be able to Ralph-loop on itself to implement its own features. Plan accordingly.
